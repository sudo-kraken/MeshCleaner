[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "MeshCleaner"
version = "0.1.0"
description = "CLI for retaining one connected component from each 3D mesh"
readme = "README.md"
keywords = ["3d-printing", "cli", "mesh", "stl", "trimesh"]
authors = [{ name = "Joe Harrison", email = "sudo-kraken@prxy.8shield.net" }]
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.9"
dependencies = [
    "trimesh>=3.23.5",
    "numpy>=1.23",
    "tqdm>=4.66.0",
    "networkx>=3.0",
]

[project.scripts]
meshcleaner = "clean_mesh:main"

[project.optional-dependencies]
dev = [
    "ruff>=0.13.1",
    "pytest>=8.3.4",
    "pytest-cov>=6.0.0",
]

[tool.setuptools]
py-modules = ["clean_mesh"]

[tool.ruff]
line-length = 120
target-version = "py39"

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "N",   # pep8-naming
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "SIM", # flake8-simplify
]
ignore = [
    "E501", # line too long (handled by formatter)
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
pythonpath = ["."]
addopts = [
    "--strict-markers",
    "--strict-config",
    "--cov=clean_mesh",
    "--cov-report=term-missing",
]

[tool.coverage.run]
source = ["clean_mesh"]
omit = ["*/tests/*"]

[tool.coverage.report]
fail_under = 90
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
]
